import styled from 'styled-components'

import COLOR from '../base-styles/color'

export default styled.hr`
  position: relative;
  border: 1px solid ${COLOR.impact.gray3};
  box-sizing: border-box;
`
